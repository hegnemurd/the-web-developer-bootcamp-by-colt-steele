console.log("Type in this code and see what happens:");
console.log(
  'const colors = ["red", "orange", "yellow", "green", "blue", "indigo", "violet"];'
);
console.log('const spans = document.querySelectorAll("span");');
console.log(
  "let index = 0; for (let color of colors) { spans[index].style.color = color; index++;}"
);
