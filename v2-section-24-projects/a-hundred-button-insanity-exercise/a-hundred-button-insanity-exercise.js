console.log("Type this code in and see what happens:");
console.log(
  'let container = document.querySelector("#container"); for (let i = 0; i<100; i++){ let buttons = document.createElement("button"); buttons.innerText = "Hey!"; container.appendChild(buttons); }'
);
