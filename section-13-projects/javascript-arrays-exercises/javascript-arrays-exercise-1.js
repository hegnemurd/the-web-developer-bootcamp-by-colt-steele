console.log(
  "I have created an array called numbers ('var numbers = [22, 67, 33, 96, 88];')"
);
console.log(
  "If you type in 'console.log(numbers[numbers.length]);' it returns undefined."
);
console.log("Undefined gets returned because there is no number there.");
console.log(
  "When you type in a number smaller (-4) or bigger (88), then that will return undefined as well since no items are occuying that spot."
);
console.log(
  "To use this piece of code correctly you need to type in an index that actually stands for an item, for example 'numbers[3]', taht will return 96, beacuse remember (it will start counting from 0)."
);

var numbers = [22, 67, 33, 96, 88];
console.log(numbers[numbers.length]);
